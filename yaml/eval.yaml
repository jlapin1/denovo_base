# Number of batches in loader is set in config.yaml:loader
eval_only:
  eval_dataset_path: /cmnfs/data/proteomics/foundational_model/9_species_V1 # None: use config.yaml setting, else: override
  eval_name: saccharomyces_cerevisiae
  set: test
  max_batches:
  save: True
  outpath: ./output.parquet
  # diffusion only
  clamp_denoised: # None -> use existing setting in loaded yaml
  n: 1 # None -> use existing
eval_train: 
  set: val
